export function getMimeType(format) {
    switch (format) {
        case 'png':
        case 'jpeg':
            return `image/${format}`;
        default:
            return 'image/svg+xml';
    }
}
